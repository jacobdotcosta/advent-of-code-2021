package jacobdotcosta;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeReactiveGreetingResourceIT extends DayOneResourceTest {

    // Execute the same tests but in native mode.
}
